#include <stdio.h>
#define MAX 100001;

char site[MAX][MAX];
char pwd[MAX][MAX];
int n, m;

int main(void)
{
	scanf("%d %d", &n, &M);

	for (int i = 0; i < n; i++)
		scanf("%s %s", site[i], pwd[i]);
	for (int i = 0; i < m; i++)
	{
		
	}
}